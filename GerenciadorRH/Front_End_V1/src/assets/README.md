# assets

Pasta reservada para imagens, ícones e outros arquivos estáticos que a
aplicação venha a precisar (por exemplo, logo em SVG, ilustrações do
portal público). Hoje nenhum asset binário é necessário — toda a
identidade visual é resolvida via Tailwind (`tailwind.config.js`) e
fontes carregadas no `index.html`.
